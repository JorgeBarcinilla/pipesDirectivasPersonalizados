import { ResaltadoDinamicoDirective } from './resaltado-dinamico.directive';

describe('ResaltadoDinamicoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltadoDinamicoDirective();
    expect(directive).toBeTruthy();
  });
});
